<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.levitg.teachmeclient">

    <!-- For Authentication -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".UserDetailActivity" />
        <activity android:name=".ListOfActivities">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ExercisesActivity" />
        <activity android:name=".ExerciseDetailActivity" />
        <activity android:name=".TeachersActivity" />
        <activity android:name=".TeacherDetailActivity" />
        <activity android:name=".CoursesActivity" />
        <activity android:name=".CourseDetailActivity" />
        <activity android:name=".SectionsActivity" />
        <activity android:name=".SectionDetailActivity" />
        <activity android:name=".LessonsActivity" />
        <activity android:name=".LessonDetailActivity" />
        <activity android:name=".ExerciseStudentsActivity" />
        <activity android:name=".ExerciseStudentDetailActivity" />
        <activity android:name=".BlobManagerActivity" />
        <activity android:name=".ListImagesActivity" />
        <activity android:name=".ImageActivity" />
        <activity android:name=".CommentsActivity" />
        <activity android:name=".CommentDetailActivity" />
        <activity android:name=".CommentRatingsActivity" />
        <activity android:name=".CommentRatingDetailActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name="com.microsoft.windowsazure.mobileservices.authentication.RedirectUrlActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="easyauth.callback"
                    android:scheme="@string/app_url_scheme" />
            </intent-filter>
        </activity>
        <activity android:name=".AzureTestLoginActivity" />
        <activity android:name=".PairsActivity" />
        <activity android:name=".PairDetailActivity"></activity>
    </application>

</manifest>